# Cool

A very simple to-do app — a single, self-contained HTML file with no
dependencies and no build step.

## Run it

Open `index.html` in any web browser. That's it.

## Features

- Add, complete, and delete tasks
- Tasks persist in your browser via `localStorage`
- "Clear completed" to tidy up
- Responsive, works on mobile and desktop
